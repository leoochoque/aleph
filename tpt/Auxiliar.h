#ifndef AUXILIAR_H
#define AUXILIAR_H

typedef char* str;
void copia_cad(str A, str B);
str devuelve_cad(str A);
int compara_cad(str A, str B);
str concat_cad(str A, str B);
int tam_cad(str A);
int compara_conj(str A, str B);
str obtenerElemento(str A);
str leeCad();

#endif
